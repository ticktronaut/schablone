============
Installation
============

from source
===========

At the command line::

    $ python setup.py install

using pypi
==========

At the command line::

    $ pip install schablone 

PiPy will follow.
